# 개발 기록

무엇을 만들었고 무엇을 확인했는지 남긴다. 특히 **작업 중에 찾은 결함**을 적는다 —
같은 함정을 다시 밟지 않기 위해서다.

## 9월 16일 — 제품화와 구조 열람

- 콘솔을 shadcn/ui + Tailwind v4 **무채색**으로 재구축했다. 화면에서 색을 갖는 것은 실행 상태 5종뿐이다.
  서류평가라 흑백 인쇄에서 손실이 없어야 하고, 유일한 색이 상태라야 시선이 이상 징후로 곧장 간다.
- 제품명을 **foldfront** 로 정하고 식별자·화면에서 발주처 이름을 걷어냈다. 기관 표기는 배포 설정으로 뺐다.
- **3차원 구조 열람**을 붙였다. 원본이 쓰던 3Dmol.js 를 승계하되 CDN 이 아니라 번들로 넣는다 —
  폐쇄망에서는 CDN 에 닿지 못한다. 서체도 같은 이유로 자체 호스팅으로 바꿨다.
  카툰 표현을 B-factor(pLDDT)로 칠해 **구조에서 색을 갖는 것이 신뢰도**가 되게 했다.
  산출물 실체 전달 경로(`GET /runs/{id}/artifacts/content`)를 새로 냈고, 등록된 산출물만 내려주며
  해석한 실제 경로가 저장 루트 안인지 다시 확인한다. 시험 4건.
- 시연 자료에 **원본 RAPID 사례연구의 실제 PDB**(`public_data/case_studies/multiround`)를 붙였다.
  지표는 모의 어댑터 값이지만 구조는 원본 산출물이다. 산출물 `meta` 에 출처를 남긴다.

### 인증·도구 표면·모델 입력

- **OIDC/SSO** — 원본 `oidc.py` 를 직접 호출한다. 토큰 검증·JWKS 회전을 다시 만들지 않는다.
  원본 역할 3종을 신규 4종으로 옮기고, **쓰기 경로 전건**에 최소 권한을 걸었다.
  모르는 역할은 조회 전용으로 떨어지므로 원본이 역할을 추가해도 권한이 넓어지지 않는다.
  OIDC 미설정 시 인증이 꺼지되 기동 로그와 `/healthz` 의 `auth` 값으로 드러난다.
- **MCP 단일 표면** — `POST /mcp` 가 원본 62종 + 신규 8종을 함께 낸다.
  이름으로 가른다. 원본 러너는 게으르게 세우고 실패를 사유로 돌려준다.
- **모델 입력 구성**(해당 기능~`007`) — `engine/payloads.py`. 원본 `clients/*` 가 실제로 보내던
  **필드 이름을 그대로** 쓰고 서열 파싱은 원본 `bio/fasta.py` 를 직접 호출한다.
  ★ 여기까지가 자격 증명 없이 확정할 수 있는 범위다. 실제 엔드포인트 연결은 `RUNPOD_API_KEY` 가 있어야 한다.

이로써 **`import pipeline_mcp` 가 6개 지점에서 실재**한다. 의존성 선언만 있던 상태가 해소됐다.

### 이때 찾은 결함

- `.gitignore` 의 `*.json` 이 `web/package.json`·`package-lock.json`·`tsconfig.json` 을 덮고 있었다.
  **clone 한 저장소에서 콘솔이 설치조차 되지 않았다.**
- 콘솔이 `/healthz` 를 직접 부르는데 개발 서버 프록시가 `/api` 만 넘겨 상태 표시등이 늘 「점검」이었다.
- 제1원칙 검사식이 커밋 본문의 파일명 `CLAUDE.md` 에 걸렸다. 꼬리말 줄로 한정해 고쳤다.


## 기반 — 데이터 계층과 API 골격

- uv 워크스페이스로 `pipeline-mcp` 를 구성원으로 묶고 `platform` 의 의존성으로 선언했다.
  도메인 로직 4,958줄(`bio/`·`clients/`)을 **복사하지 않는다** — 원본 파일을 고치지 않으므로
  upstream 변경분을 언제든 병합한다.
  ★ 현재는 **의존성 선언까지다.** `platform/` 안에 `import pipeline_mcp` 는 아직 0건이고,
  실제 호출은 모델 어댑터를 실제 엔드포인트에 연결할 때 들어간다.
  모델 단계를 「부분 구현」으로 두는 이유가 이것이다.
- 문서 스키마 14종 · 인덱스 43개.
- `PipelineRequest` 는 필드가 **127개**이고 계속 늘어나므로 `request` 를 통째로 보존하고
  조회에 쓰는 값만 승격했다. 현행 필드 이름을 바꾸지 않은 것이 해당 기능 의 전제다.
- FastAPI 로 OpenAPI 3.1 명세를 낸다. 현행 배포 절차가 보는 `/healthz` 경로는 같게 뒀다.

## 저장소 계층과 레거시 이관

- 저장소 12종을 `Repos` 하나로 묶었다.
- `fork` 를 기본 동작으로 구현했다. 원본 run 은 손대지 않는다.
- 작업 큐를 lease 방식으로 만들었다. `find_one_and_update` 가 원자적이라 경합이 없다.
- 레거시 마이그레이션 — 현행 run 디렉토리를 읽어 옮기되 **원본은 지우지 않는다.**
  현행 `status.json` 의 단계 표현이 사전·목록 두 갈래라 둘 다 받는다. 피드백의
  `verdict`·`rating`, `good`·`bad` 표기 흔들림도 정규화한다. 다시 돌려도 중복되지 않는다.

## 자유형 DAG 엔진

현행에 DAG 코드가 **0건**이다. 이 사업의 승부처다.

- 위상 정렬로 실행 순서를 정하고 같은 층은 병렬 실행 대상으로 묶는다.
- 그래프 검증을 **저장 시점**에 한다 — 순환·중복 식별자·없는 노드 참조·자기 간선·
  조건식 없는 분기 노드.
- 조건식은 파이썬 `eval` 을 쓰지 않는다. 화면에서 들어온 문자열이므로 경로·비교연산자·수
  형태만 받는다(해당 기능 취지).
- 엔진은 실행하지 않고 **무엇을 돌릴지만** 정한다. 같은 엔진으로 모의 실행과 실제 실행을 돌린다.
- 현행 고정 체인을 DAG 템플릿으로 표현했다 — 해당 기능 와 해당 기능 이 한 엔진 위에서 돈다.

### 발견한 결함

**합류 노드가 영구 정체됐다.** `_blocked` 는 부모 하나만 건너뛰어도 막는데 `_cascade_skips` 는
모든 부모가 죽어야 건너뛰어, 조건 분기 뒤의 합류 노드가 실행되지도 건너뛰어지지도 않았다.
합류 의미론을 둘로 나눠 고쳤다 — **JOIN 은 OR 합류**(하나라도 살아 있으면 실행),
**그 밖은 AND 합류**(모두 성공해야 실행). 회귀 시험 3건.

## 라우팅 · 실행 서비스 · API

- 동적 라우팅 — 모델 ID 를 실행 엔드포인트로 바꾼다. RunPod 우선, 다음 HTTP 워커,
  그다음 컨테이너. 비활성·미승인·자원 초과 버전은 라우팅하지 않는다.
- 실행 서비스가 엔진과 큐를 잇는다. 워커는 상태를 들고 있지 않고 `load_plan` 이 DB 에서
  계획을 복원한다.
- HTTP API **24경로** · OpenAPI 3.1. 현행 MCP 도구 이름을 경로에 대응시켰다.

### 발견한 결함

**제어 노드 통과가 무한 재귀했다.** 이미 통과시킨 노드가 재귀 조건을 계속 만족했다.
루프로 바꾸고 처리한 노드를 기억하게 했다. 첫 노드부터 라우팅에 실패하면 run 이
`RUNNING` 으로 방치되던 것도 함께 고쳤다.

## 모델 어댑터와 워커

- 어댑터 셋 — RunPod 서버리스 · HTTP 워커 · 모의.
- 모의 어댑터는 단계마다 조건식이 참조할 지표를 낸다(`soluprot.pass_rate`·`af2.plddt`).
  ⚠️ **모의 결과를 「동작한다」로 기재하지 않는다.** 실증은 실제 엔드포인트로 한다.
- 워커는 `lease → invoke → complete_node` 를 돈다. 죽었다 살아나도 DB 에서 이어서 돈다.
- 워커 여러 개를 동시에 띄워도 같은 작업을 두 번 집지 않는다.
- GPU 를 못 쓰는 워커는 자원 요구량이 큰 작업을 건너뛴다.

### 발견한 결함

**조건 분기 노드가 큐에 들어가 실패했다.** `BRANCH` 는 조건만 평가하고 `FANOUT`·`JOIN` 은
흐름만 가른다 — 셋 다 모델이 없으므로 큐에 넣으면 워커가 호출할 대상이 없다.
제어 노드로 취급해 큐를 거치지 않게 고쳤다.

## React 통합 콘솔

- 화면 일곱을 한 벌로 묶었다 — 프로젝트·Setup·Workflow Studio·Monitor·Analyze·
  Model Registry·운영.
- **프로젝트와 재설계 회차** — 원본 이름이 *Redesign* 인데 재설계 반복이 화면에 없었다.
  API·DB·색인은 이미 있었고 쓰이지 않았을 뿐이다. 머리말 선택기 하나가 대시보드·실행 감시·
  결과 분석의 조회 범위를 정하고, 실행은 고른 회차에 기록된다.
  `?project=…` 로 링크를 걸 수 있다.
- **입력 파일** — 브라우저에는 서버 경로를 알 방법이 없었다. 서열·구조 칸이 붙여넣기·
  끌어다 놓기·경로 세 가지를 받고, 받은 것을 요약해 되돌려 준다(서열 N개 · 잔기 수 · 체인).
  구조 파일을 놓으면 체인 목록이 채워진다. 올린 파일의 이름은 라벨로만 쓰고 경로는 서버가 만든다 —
  경로 조작이 잡히는 게 아니라 불가능하다. `POST /inputs`.
- **알림** — 사람 손이 필요한 것만 울린다. 승인 대기 모델 · 최근 실패한 실행 ·
  30분째 변화 없는 실행 · 인증이 꺼진 채 도는 설치. 무엇을 낼지는 서버가 역할을 보고 정한다 —
  조회자에게 승인 대기를 보여 줘도 눌러 봐야 할 수 있는 일이 없다.
- **DAG 편집기** — 노드를 끌어 놓고 간선을 이어 흐름을 설계한다.
  조건 분기는 나가는 간선에 참·거짓을 지정한다. 저장하면 서버가 그래프를 검증하고
  실행 층 구조와 미등록 모델을 돌려준다.
- **노드 편집 패널** — 노드를 더블클릭하면 캔버스 위에 겹쳐 뜬다. 노드 종류 다섯이
  한 패널을 나눠 쓰고, 다른 노드를 누르면 닫히지 않고 내용만 바뀐다.
  주소에 노드를 적어(`/studio?wf=…&node=…`) 특정 노드를 바로 열 수 있다.
- **점검** — 저장 전에 빠진 것을 짚는다. 모델 미지정 · 빈 조건식 · 참·거짓 미지정 ·
  고립된 노드 · 자기 자신으로 가는 간선. 빠진 것이 없으면 패널 자체가 사라진다.
- API 호출을 `client.ts` 한 곳에 모았다. 현행은 `fetch` 가 화면 코드 18곳에 흩어져 있었다.

### 발견한 결함

**DAG 간선이 노드 안으로 말려 들어갔다.** 흐름은 왼쪽에서 오른쪽인데 ReactFlow 연결점이
상하 기본값이었다. `sourcePosition`·`targetPosition` 을 좌우로 지정해 고쳤다.

**프로젝트로 거르면 워크플로가 전부 사라졌다.** 공용 템플릿은 `project_id` 가 없어서
`{"project_id": 고른 값}` 에 걸리지 않는다. 「자기 것 + 공용」으로 바꿨다 —
템플릿이 안 보이는 스튜디오는 고장난 것으로 읽힌다.

**머리말의 「프로젝트」와 「알림」이 아무 동작도 없는 장식이었다.** 둘 다 실제로 동작하게 바꿨다.

**적대적 검토 3건이 29건을 재현했다 (2026. 9. 16.).** 엔진·업로드 보안·화면 정확성 세 관점으로
「반증하라」고 시켰고, 커밋 메시지가 코드가 지키지 않는 약속을 셋 했다는 것이 드러났다.

| 드러난 것 | 고친 것 |
|---|---|
| `reclaim_loop` 를 아무도 시작하지 않았다 — 정합성 점검이 "60초마다" 돈 적이 없다 | `cmd_worker` 가 두 루프를 함께 돌린다 |
| `run_events` 번호를 읽고-넣기로 매겨 워커 둘이 충돌했다(30회에 1회). 간헐 실패를 「일시적」이라 넘겼던 것 | run 문서에서 `$inc` 로 원자 발급 |
| 정합성 점검이 진행 중인 워커와 경합해 다음 노드를 두 번 큐에 넣었다 | 활성 job 에 (run, node) 부분 유일 색인 — 둘째 삽입은 실패한다 |
| 늦게 끝난 워커가 닫힌 run 을 SUCCEEDED 로 뒤집었다 | `complete_node` 가 끝난 run 을 거부하고 사건만 남긴다 |
| 점검이 시작한 적 없는 fork(PENDING)를 실행했다 | RUNNING 만 점검한다 |
| 엔진이 **서버의 아무 파일이나** 읽어 FASTA 헤더를 metrics 로 노출했다 | 저장 위치 안의 경로만 읽는다 |
| 32MB 상한이 본문 전체를 디스크에 쓴 뒤 적용됐다 | Content-Length 를 먼저 보고 거절, 없으면 411 |
| 붙여넣은 내용은 상한 없이 모든 job 에 복제됐다 | 1MB 넘으면 파일로 올리라고 거절 |
| 경로/내용 판정이 화면과 서버에서 달랐다(경로 뒤 Enter) | 둘 다 「공백 유무」로 판정 |
| Setup 이 다른 프로젝트의 회차에 기록했고, 「회차 없이」는 고를 수 없었다 | 프로젝트 바뀌면 초기화, null/'' 구분, 서버도 소속 검사 |
| `.cif` 를 PDB 로 읽어 없는 체인을 채웠고, 체인 자동 채움이 사용자 편집을 덮었다 | mmCIF 는 요약 안 함, 손댄 뒤엔 안 덮음 |
| 거부된 모델이 승인 대기로 영원히 울렸다 | `pending` 만 울린다 |
| 조회자에게 403 나는 「되살리기」가 보였고 실패가 삼켜졌다 | 권한 있을 때만, 실패는 보인다 |
| `?project=` 가 새로고침마다 되살아나 사용자 선택을 덮었다 | 도착 시 한 번만 읽는다 |

수정분을 다시 검토시켜 9건이 더 나왔다(배포 전 run 의 사건 번호 충돌 · cwd 에 따라 달라지는 저장 위치 ·
쌍둥이 job 이 있는 저장소에서 색인 생성 실패 등). 전부 고쳤고 `8974778`.

남긴 결정 둘 — 업로드 파일의 보존 기간·용량 한도(지금은 무한), fork 한 실행을 누가 시작하는가(지금은 경로 없음).

**실행 하나가 59분째 「실행 중」으로 멈춰 있었다.** 알림을 붙이고 나서야 드러났다.
run 과 job 을 따로 쓰므로 둘이 어긋날 수 있고, 어긋나면 그 실행은 다시 움직이지 않는다.

| 어긋나는 길 | 지금까지 |
|---|---|
| 워커가 job 을 먼저 쓰고 stage 를 쓴다 — 그 사이에 죽으면 | 일은 끝났는데 실행은 계속 기다린다 |
| `reclaim_expired` 가 lease 만료 job 을 실패시킨다 | job 만 본다. run 을 실패시킬 방법이 없다 |
| job 이 통째로 사라진다 | 아무도 보지 않는다 |

**정합성 점검(`reconcile`)** 을 넣어 셋 다 닫았다. 없는 결과를 지어내지 않는다 —
job 이 실제로 남긴 것만 stage 로 옮기고, job 이 없는 노드는 다시 큐에 넣는다.
결과를 잃은 job 은 이어붙이지 않고 실패시킨다. 빈 결과를 다음 단계에 먹이고 성공이라 부르는 것보다 낫다.
워커가 60초마다 돌리고, 운영 화면과 알림의 「되살리기」로도 부른다.
실측 — 멈춰 있던 `run-71042d0a5c14` 이 재큐 후 성공으로 닫혔다.

## 통합과 실행 검증

- CLI 를 넣었다 — `seed` · `demo` · `worker` · `migrate` · `status`.
- **결합 예측 워크플로**를 기본 템플릿으로 넣었다. 용해도 통과율이 낮으면
  도킹을 건너뛰는 조건 분기를 둬서 계산 자원을 아끼는 실제 운영 방식을 담았다.
- 실제 서버·MongoDB 로 **실행 6건 · 작업 39건**을 돌렸다.
- 화면 캡처 9종을 `docs/shots/` 에 남겼다.
- 기능별 동작 상태를 정리했다.
  `tools/verify_matrix.py` 로 다시 뽑는다.

## 규모

| 상태 | 종수 | 비율 |
|---|---|---|
| **구현 완료** | 37 | 52% |
| **부분 구현** | 20 | 28% |
| **사업 기간 내 구현** | 14 | 19% |

★ **「구현 완료」는 모의 어댑터가 아니라 기능 자체가 동작함을 뜻한다.**
모델 실행은 실제 GPU 엔드포인트 연결 전이므로 관련 항목을 「부분 구현」으로 두었다.
**이 구분을 문서에 그대로 옮긴다** — 부풀리면 나중에 거짓이 된다.

## 시험

```bash
docker compose -f platform/docker-compose.yml up -d    # MongoDB
uv run pytest platform/tests -q                        # 324건
cd web && npm test                                     # 124건
cd web && npm run e2e                                  # 전 과정 1건 (브라우저, 실제 워커)
```

현재 **448건 통과**(백엔드 324 · 화면 124) + **전 과정 통합 시나리오 1건**(Playwright, 11단계, 6초).
백엔드 시험은 실제 MongoDB 에 붙어 돈다. 통합 시나리오는 `docs/e2e/` 에 단계별 캡처를 남긴다.

## 남은 일

1. **RunPod 실제 엔드포인트 연결** — 소액으로 모델 1~2종을 실제 호출하고
   `run_id`·실행 시각·산출물을 증적으로 남긴다. 모의 결과로는 「동작한다」를 쓸 수 없다.
2. Copilot과 Agent Panel 승계 연결.
3. 3D 구조 뷰어 연결.
4. 부하 시험으로 해당 기능 동시 50명·3초 실측.

## 검사

```bash
git log upstream-v1.0.29..HEAD --format='%an|%ae|%B' | grep -icE 'claude|anthropic|co-authored|🤖'
gh repo view mhb8436/foldfront --json visibility
gh workflow list -R mhb8436/foldfront --all
```
